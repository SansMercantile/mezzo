# Set once the Mezzo backend is actually deployed (Lambda + API Gateway,
# or wherever it ends up running). Without this, the Setup dashboard tab
# shows "Backend not configured yet" instead of erroring.
VITE_MEZZO_API_BASE_URL=
